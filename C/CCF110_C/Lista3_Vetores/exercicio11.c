#include <stdio.h>
#include <stdlib.h>

int main(){
                int vetor[30],i,A;
    printf("Digite um numero: ");
    scanf("%d",&A);
    for (i = 0; i < 30; i++){
        printf("Digite um numero: ");
        scanf("%d",&vetor[i]);
        getchar();
        
        }
        }
    }
    printf("%d são positivos e %d são negativos",positivo,negativo);  

    return 0;
}